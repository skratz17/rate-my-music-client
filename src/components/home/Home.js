import React from 'react';

import { Page } from '../common';
import { FullSearch } from '../search/FullSearch';

export const Home = () => {
  return (
    <Page>
      <FullSearch />
    </Page>
  );
};